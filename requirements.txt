# SBA File Renamer — Python dependencies
# Install:  pip install -r requirements.txt

# PDF text extraction (fast, text-based PDFs)
pypdf>=4.0.0

# OCR fallback for scanned PDFs
pdf2image>=1.17.0      # wraps poppler's pdftoppm
pytesseract>=0.3.10    # wraps Tesseract-OCR

# Excel / CSV master list
pandas>=2.0.0
openpyxl>=3.1.0        # xlsx engine for pandas
xlrd>=2.0.1            # legacy .xls engine

# Web deployment
flask>=3.0.0

# Desktop build (only needed for packaging)
# pyinstaller>=6.0.0
